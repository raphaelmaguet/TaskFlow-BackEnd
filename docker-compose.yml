version: '3.9'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: taskflow-server
    ports:
      - '3000:3000'
    env_file:
      - .env
    restart: unless-stopped
